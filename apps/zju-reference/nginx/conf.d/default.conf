# Production reverse proxy for the game frontend, backend API, WebSocket, admin,
# and intentionally public world-data reference files.
server {
    listen 80;
    server_name 67656.fun;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name 67656.fun;

    ssl_certificate      /etc/nginx/ssl/67656.fun.crt;
    ssl_certificate_key  /etc/nginx/ssl/67656.fun.key;

    ssl_protocols        TLSv1.2 TLSv1.3;
    ssl_ciphers          HIGH:!aNULL:!MD5;

    limit_req zone=req_limit_per_ip burst=20 nodelay;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    location /world/ {
        alias /usr/share/nginx/world/;
        autoindex on;
    }

    location /ws/ {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location = /admin {
        return 301 /admin/;
    }

    location /admin/ {
        proxy_pass http://zjus_backend:8000;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        root /usr/share/nginx/game-frontend;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
