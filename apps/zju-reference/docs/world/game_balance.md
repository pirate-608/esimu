# 游戏数值平衡 v1.3.0
配置来源：world/game_balance.json（2026-02-01 节奏优化版）。

属性字段、默认值和道具/事件 effect 白名单见[属性定义](/world/stat_definitions)；本页只记录节奏、概率、课程状态、休闲动作和考试等平衡参数。

## 学期节奏
- 前 4 学期各 420 秒，后 4 学期各 300 秒，总时长约 48 分钟。
- 默认单局节奏：360 秒。

| 学期 | 时长（秒） |
| ---- | ---------- |
| 1    | 420        |
| 2    | 420        |
| 3    | 420        |
| 4    | 420        |
| 5    | 300        |
| 6    | 300        |
| 7    | 300        |
| 8    | 300        |

### 速度模式
| 模式 | 标签      | 倍率 |
| ---- | --------- | ---- |
| 1.0  | 正常速度  | 1.0  |
| 1.5  | 1.5x 加速 | 1.5  |
| 2.0  | 2x 加速   | 2.0  |

## Tick 基础
- 间隔：3 秒
- 基础精力消耗：0.4
- 基础 mastery 增长：0.5

`tick.interval_seconds` 会被 `GameEngine.run_loop()` 直接使用。默认 3 秒不改变当前手感；如果在后台调低，会提高 Redis 读写、WebSocket 推送和事件检查频率；如果调高，则会让游戏节奏整体变慢。

## 课程状态
| 状态 | 名称 | Emoji | 成长 | 消耗 |
| ---- | ---- | ----- | ---- | ---- |
| 0    | 摆   | 💤     | 0.0  | 0.0  |
| 1    | 摸   | 😐     | 1.0  | 0.8  |
| 2    | 卷   | 🔥     | 4.0  | 3.0  |

## 心态与压力修正
- 成长阶段
	- 心态：<20 时 0.6 倍；>80 时 1.2 倍；低区斜率 0.013，高区斜率 0.007。
	- 压力：区间 40~70 时 1.3 倍；区间外 0.85 倍；极端 0.6 倍。
- 考试阶段
	- 心态：低区斜率 0.3，高区斜率 0.12，优秀加成 +6。
	- 压力：最佳 +6，次优 -5，极端 -10。

## 放松行为
| 行为 | 标签       | 精力消耗 | 精力回复 | 心态变化 | 压力变化 | 冷却（秒） | 额外要求/效果                                     |
| ---- | ---------- | -------- | -------- | -------- | -------- | ---------- | ------------------------------------------------- |
| gym  | 健身房     | -30      | +40      | +5       | -5       | 30         | 需精力 >= 30；20% 概率魅力 +1                    |
| game | 打游戏     | -5       | -        | +20      | -        | 15         | -                                                 |
| walk | 散步启真湖 | -        | -        | -        | -10      | 15         | -                                                 |
| cc98 | 刷CC98     | -        | -        | 概率事件 | 概率事件 | 15         | 0.55:心态+8压-5；0.25:心态+2压-1；0.2:心态-8压+6 |

休闲动作的正向收益若被属性端点吞掉，会把最多 20 点溢出收益转移到精力、心态、魅力，且单次转移到魅力最多 +1；该规则只作用于休闲动作，不改变随机事件、钉钉结算或道具被动。

## 事件
| 事件         | 检查间隔（tick） | 触发概率 | 额外配置 |
| ------------ | ---------------- | -------- | -------- |
| random_event | 12               | 0.5      | - |
| dingtalk     | 12               | 0.5      | `max_contacts=12`；`reuse_closed_contact_probability=0.7` |

## 考试
- 不及格阈值：60
- 每门挂科心态：-10
- 全部及格心态：+10

## 结局判定
- 心态阈值：0
- 精力阈值：0

## 后台管理

管理员可在 `/admin/balance` 图形化编辑本文件中的既有数值字段。保存时后端会：

1. 校验完整配置。
2. 用临时文件 + `os.replace` 原子替换 `world/game_balance.json`。
3. 调用 `balance.reload()` 热重载。
4. 在 `admin_audit_logs` 中记录 `balance_update`。

“恢复上一版”读取最近一次 `balance_update` 保存前的旧配置并写回，随后记录 `balance_restore`。道具目录由 `/admin/items` 单独维护，属性定义、课程和事件库仍通过世界数据文件与校验脚本维护。

生产环境默认挂载 `./zjus-backend/world:/app/world`，因此后台保存会写入服务器上的挂载目录，而不是只改容器镜像内部文件。

更多维护流程见[游戏设定维护](/dev/world-data)。
