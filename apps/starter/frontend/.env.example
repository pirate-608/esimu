# Leave these empty when the frontend and backend share an origin or Vite proxy.
VITE_ESIMU_API_BASE=
VITE_ESIMU_WS_BASE=

# Used only by the local Vite development proxy.
ESIMU_DEV_BACKEND_URL=http://127.0.0.1:18001